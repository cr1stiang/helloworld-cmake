#include <iostream>
#include "hello.h"

int main (void){

    int in1;

    std::cin >> in1;
    std::cout << "I'm cristi " << in1;
}
// go to build folder : cd ./build
// build executable : cmake --build .
// run executable : ./app